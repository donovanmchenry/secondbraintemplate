# Local Project Paths

Copy this file to `projects/local-paths.md` only when connecting projects outside the
Second Brain folder. The copied file is gitignored because paths reveal machine-specific
information and differ across computers.

```text
- `example-project`: `/path/to/example-project`
- `another-project`: `C:\path\to\another-project`
```
