# Runtime

Automation may write local status and run history in this folder. Runtime files are
gitignored because they can expose timestamps, paths, account names, or live state.

Each run record should include its timestamp, routine, sources checked, actions
taken, and warnings. Promote only confirmed reusable knowledge into the wiki.
